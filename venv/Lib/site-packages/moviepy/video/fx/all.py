"""
This module imports all the standard effects so that they can be accessed
directly, e.g. via `moviepy.video.fx.all.fadein`.
"""

from moviepy.video.fx.fadein import fadein
from moviepy.video.fx.fadeout import fadeout
from moviepy.video.fx.resize import resize

__all__ = ["fadein", "fadeout", "resize"]
