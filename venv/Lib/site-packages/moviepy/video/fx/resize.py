def resize(clip, newsize):
    return clip.resize(newsize)

# 임시 클래스 추가
class Resize:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("Resize class is not implemented. Use resize() instead.")
